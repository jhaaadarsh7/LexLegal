export interface SfsSearchResult {
  id: string;
  title: string;
  sfsNumber: string;
  publishedAt?: string;
}

export interface SfsDocument {
  id: string;
  title: string;
  sfsNumber: string;
  text: string;
}
